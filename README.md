# My Planner

A basic static React layout for a planner app. Nothing is wired up yet, so it is a clean starting point for learning login, backend routes, and SQL later.

## Run

```bash
npm run dev
```
